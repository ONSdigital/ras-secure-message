language: python
sudo: required
dist: trusty
python: '3.6'

# before_install:

cache:
    - pip

install:
    - pip install pipenv
    - pipenv install --dev --deploy

script:
    - pipenv run coverage run run_tests.py

services:
    - postgresql

after_success:
    - pipenv run codecov

branches:
    only:
        - master

addons:
  postgresql: "9.6"
